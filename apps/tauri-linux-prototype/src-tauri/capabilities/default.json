{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "prototype-main",
  "description": "Only window close for the active-work drain probe; no shell, process, filesystem, or HTTP plugin",
  "windows": ["main"],
  "permissions": ["core:window:allow-close"]
}
